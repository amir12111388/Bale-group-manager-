# آپدیت پاکسازی گروه

## امکانات

- دستور `پاکسازی گروه`
- دستور `پاکسازی 100`
- دستور `پاکسازی گروه 1000`
- دستور `پاکسازی گروه کل`
- تایید شیشه‌ای قبل از شروع پاکسازی
- حذف پیام‌ها به صورت صدتا صدتا
- نمایش تعداد پیام‌های بررسی‌شده، حذف‌شده و ناموفق
- ذخیره شناسه پیام‌های جدید برای پاکسازی‌های بعدی

## تنظیمات اختیاری در `.env`

```env
PURGE_CHUNK_SIZE=100
PURGE_DEFAULT_SCAN=1000
PURGE_MAX_SCAN=10000
PURGE_SLEEP_SECONDS=0.06
```

> نکته: ربات فقط پیام‌هایی را می‌تواند حذف کند که بله اجازه حذف‌شان را بدهد و ربات دسترسی حذف پیام داشته باشد.
