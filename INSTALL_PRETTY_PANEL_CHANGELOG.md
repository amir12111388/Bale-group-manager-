# Install + Pretty Panel Update

اضافه شد:

- وقتی ربات به گروه اضافه شود، پیام راه‌اندازی می‌دهد و می‌گوید دستور `نصب` را بزنید.
- دستور فارسی `نصب`، `راه اندازی` و `فعال سازی` اضافه شد.
- قبل از باز شدن پنل، اگر گروه نصب نشده باشد، ربات راهنمای نصب نشان می‌دهد.
- پنل اصلی تمیزتر، دسته‌بندی‌شده‌تر و واضح‌تر شد.
- دکمه «نصب / بررسی» برای چک‌کردن نصب و دسترسی ربات اضافه شد.
- بخش‌های جدید پنل: راهنمای سریع، کاربران، ضدتبلیغ هوشمند.
- دکمه‌های کپی دستور برای نصب، پنل، قفل لینک و... اضافه شد.

