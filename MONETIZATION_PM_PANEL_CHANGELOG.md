# تغییرات آپدیت پنل مالک و خرید پیوی

این نسخه جریان فروش اشتراک را کامل‌تر کرد:

- تنظیم شماره کارت از داخل پیوی ربات، بخش «پنل مالک»
- تنظیم نام صاحب کارت از داخل پیوی
- تنظیم متن راهنمای کارت‌به‌کارت از داخل پیوی
- تنظیم توکن/مرچنت پرداخت بله از داخل پیوی
- روشن/خاموش کردن پرداخت بله و کارت‌به‌کارت از پنل مالک
- مدیریت پلن‌ها از پیوی مالک: افزودن، روشن/خاموش، حذف
- خرید و تمدید اشتراک فقط داخل پیوی ربات
- داخل گروه فقط وضعیت اشتراک و پلن‌ها نمایش داده می‌شود
- ثبت سفارش و رسید کارت‌به‌کارت داخل پیوی
- ارسال رسیدهای در انتظار برای مالک با دکمه تایید/رد
- فعال‌سازی خودکار اشتراک بعد از تایید رسید
- فعال‌سازی خودکار اشتراک بعد از پرداخت موفق بله
- داشبورد فروش، سفارش‌ها، رسیدها و گروه‌های فعال در پیوی مالک

## دستورهای جدید

داخل پیوی مالک:

```text
پنل
پنل مالک
پنل فروش
```

داخل پیوی کاربران:

```text
خرید اشتراک
خرید اشتراک -100123456789
```

داخل گروه:

```text
اشتراک
```

داخل گروه دیگر پرداخت انجام نمی‌شود؛ فقط وضعیت اشتراک و دکمه رفتن به پیوی نمایش داده می‌شود.
