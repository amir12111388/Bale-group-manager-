# آپدیت پاکسازی سریع

- وقتی پاکسازی ۵۰۰/۱۰۰۰ یا کل زده می‌شود، اگر چند پیام پشت‌سرهم قابل حذف نباشد، ربات تا آخر عدد اسکن نمی‌کند و سریع نتیجه می‌دهد.
- روی پیام‌های ناموفق دیگر sleep/delay اعمال نمی‌شود.
- متن نهایی ساده‌تر شد: «پاک شد» + تعداد حذف‌شده/بررسی‌شده.
- مقدار قابل تنظیم جدید: `PURGE_STOP_AFTER_CONSECUTIVE_FAILS` با پیش‌فرض 35.
