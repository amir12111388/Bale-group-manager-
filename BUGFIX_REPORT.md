# گزارش تبدیل و رفع ایرادها

## مواردی که از سورس تلگرام حذف/بازطراحی شد

1. وابستگی به Pyrogram/Kurigram و Userbot حذف شد.
2. `SESSION_STRING` و اجرای اکانت کاربری حذف شد تا ریسک بن/لو رفتن سشن وجود نداشته باشد.
3. دستورات خطرناک `/eval` و `/sh` حذف شد.
4. MongoDB و pickle حذف شد؛ SQLite/SQLJSON جایگزین شد.
5. `os.system` برای بکاپ حذف شد؛ بکاپ امن ZIP با API خود SQLite نوشته شد.
6. Flood قبلی که فقط در RAM بود، به enforcement سبک RAM + لاگ دیتابیس تبدیل شد.
7. ماژول‌های غیرضروری مثل music، crypto، rss، downloader، arq و googletrans حذف شدند تا سورس نصب‌پذیر بماند.
8. خطاهای async/await مربوط به کتابخانه قبلی دیگر وجود ندارد، چون کلاینت HTTP ساده و sync نوشته شده است.
9. تنظیمات گروه‌ها یکپارچه و JSON-based شد؛ تنظیمات پراکنده در چند فایل حذف شد.
10. پرداخت بله به‌صورت اسکلت آماده در `BaleAPI.send_invoice` و هندل `pre_checkout_query` لحاظ شد، ولی برای این ربات مدیریت گروه پیش‌فرض خاموش است.

## ایرادهای ساختاری که رفع شد

- نبود پکیج نصب تمیز → `requirements.txt`, `.env.example`, `run.sh`, `systemd installer` اضافه شد.
- نداشتن بکاپ امن → `/backup` اضافه شد.
- نبود تقویم شمسی → `jdatetime` اضافه شد.
- نبود ادمین داخلی ربات → `/addadmin`, `/deladmin`, `/admins` اضافه شد.
- نبود جوین اجباری پایدار → جدول `required_channels` اضافه شد.
- کش دائمی عضویت → استفاده نشده؛ جوین اجباری در هر تعامل دوباره چک می‌شود.
- عدم سازگاری تلگرام/بله → همه متدها به HTTP API بله روی `https://tapi.bale.ai/bot<TOKEN>/METHOD` منتقل شد.

## محدودیت آگاهانه

بله در مستندات فعلی متد `restrictChatMember` را مانند تلگرام ارائه نکرده است؛ به همین دلیل mute واقعی تلگرامی به shadow mute تبدیل شد: کاربر در دیتابیس میوت می‌شود و هر پیام او در گروه حذف می‌شود.
