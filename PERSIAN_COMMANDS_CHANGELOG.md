# گزارش فارسی‌سازی دستورات

در این نسخه دستورات اصلی ربات مدیریت گروه بله فارسی شدند و دیگر لازم نیست حتماً از دستورهای اسلش‌دار انگلیسی استفاده شود.

## نمونه‌های اضافه‌شده

```text
قفل لینک
باز کردن لینک
لینک قفل شه
لینک باز شه
قفل همه
باز کردن همه
بن
رفع بن
اخراج
اخطار دلیل
حذف اخطار
میوت 10 دقیقه دلیل
رفع میوت
تنظیم خوشامد متن
خوشامد روشن
خوشامد خاموش
تنظیم قوانین متن
جوین اجباری افزودن @channel عنوان
جوین اجباری حذف @channel
افزودن فیلتر کلمه | پاسخ
افزودن نوت نام | متن
پنل
راهنما
آمار
بکاپ
آیدی
```

## موارد فنی اصلاح‌شده

- parser جدید برای دستورات فارسی بدون اسلش اضافه شد.
- اعداد فارسی و عربی به عدد انگلیسی تبدیل می‌شوند.
- نیم‌فاصله و تفاوت «ي/ی» و «ك/ک» نرمال‌سازی می‌شود.
- مدت زمان فارسی مثل `10 دقیقه`، `2 ساعت` و `1 روز` برای میوت پشتیبانی می‌شود.
- دستورات انگلیسی نسخه قبلی حفظ شدند.

## نسخه قفل‌های کامل + پنل شیشه‌ای

در این نسخه همه قفل‌های کاربردی مدیریت گروه به صورت فارسی و با دکمه‌های شیشه‌ای اضافه شد.

### قفل‌های اضافه‌شده

- قفل لینک، لینک بله، لینک تلگرام، لینک اینستاگرام، لینک واتساپ، لینک سایت
- قفل کانال، آیدی/یوزرنیم، منشن، شماره، تبلیغ
- قفل رسانه، عکس، ویدیو، ویس، موزیک/صدا، فایل، استیکر، گیف، مخاطب، لوکیشن
- قفل فوروارد، فلود، پیام تکراری، متن طولانی، پیام کوتاه، ایموجی زیاد، حروف کشیده، فحش
- قفل ربات، اکانت فیک، اسم مشکوک، عضو جدید
- قفل پیام سرویس، پیام ورود، پیام خروج، تغییر عنوان، تغییر عکس گروه، چت، شبانه

### پنل

با دستور `پنل` وارد بخش قفل‌ها شوید. هر قفل با یک دکمه شیشه‌ای روشن/خاموش می‌شود.

### قفل فحش

برای قفل فحش، لیست کلمات را در `.env` تنظیم کنید:

```env
BAD_WORDS=کلمه۱,کلمه۲,کلمه۳
```
