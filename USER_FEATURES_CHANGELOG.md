# تغییرات آپدیت کاربرمحور

این نسخه برای حرفه‌ای‌تر شدن تجربه اعضای گروه اضافه شده است.

## اضافه شد

- پروفایل کاربر با دستور «پروفایل من»
- امتیاز و سطح‌بندی کاربران
- لیدربورد/فعال‌ترین‌ها با دستور «فعال‌ها»
- نمایش اخطارهای شخصی با دستور «اخطارهای من»
- گزارش تخلف با ریپلای روی پیام و نوشتن «گزارش»
- ارسال گزارش برای مدیرها با دکمه تایید/رد
- اعتراض به اخطار و میوت از پیوی ربات
- دکمه «اعتراض دارم» زیر اخطار/میوت دستی
- راهنمای هوشمند دکمه‌ای
- ریست جداگانه متن‌های سفارشی
- ریست جداگانه اسم دستورهای سفارشی
- ریست جداگانه متن قفل‌ها
- ریست کامل شخصی‌سازی گروه

## دستورهای جدید داخل گروه

```text
پروفایل من
پروفایل          ← با ریپلای، مدیر می‌تواند پروفایل کاربر را ببیند
اخطارهای من
فعال‌ها
گزارش            ← با ریپلای روی پیام متخلف
راهنما
```

## ریست شخصی‌سازی از پیوی

در پیوی ربات برای گروه موردنظر:

```text
ریست متن‌ها -100123456789
ریست دستورها -100123456789
ریست شخصی سازی -100123456789
```

یا از مسیر:

```text
پنل گروه ← 🎨 شخصی‌سازی ← ورود به پیوی ← دکمه‌های ریست
```

## نکته

ریست متن‌ها و دستورها فقط از پیوی انجام می‌شود تا داخل گروه شلوغ نشود و هر مدیر اشتباهی تنظیمات را تغییر ندهد.
